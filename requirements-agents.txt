# Dependencies for the background agents (NOT needed on Vercel)
# Install locally with: pip install -r requirements-agents.txt

feedparser>=6.0.11
requests>=2.31.0
schedule>=1.2.1
resend>=2.0.0

# translation_agent.py only
transformers>=4.40.0
torch>=2.2.0
